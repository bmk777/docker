To run the client application

docker pull bmk77/workingproject:1
docker run -it workingproject:1
Follow the on screen instructions to continue

Plan to connect to GCP
Extend terminal functionality to replicate running the program in the GCP terminal
For file location
	User selects case one of the menu.
	//Execute gcloud $some authentication protocol
	//Create a temporary bucket and store the file location inside
	//Print out command to access file locatin

For word
	User selects case two of the menu
	//Check the user is authenticated
	//navigate to file location
	//implement sort/find scheme (inverted)
	//return data: Frequence, ect..